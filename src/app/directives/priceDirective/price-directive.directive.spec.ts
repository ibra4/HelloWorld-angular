import { PriceDirectiveDirective } from './price-directive.directive';

describe('PriceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
